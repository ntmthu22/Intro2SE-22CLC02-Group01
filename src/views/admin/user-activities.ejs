<%- include('../includes/general/head.ejs') %>
    <style>
      @media only screen and (max-width: 991px) {
        /* For mobile phones: */
        .btn.btn-info.btn-lg.btn-block {
          margin-bottom: 1rem;
        }
        section {
          margin-bottom: 2rem;
        }
      }
      .custom-link:hover{
        color: #0d6efd;
        text-decoration: underline;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <%- include('../includes/general/navbar.ejs') %>
    
    <section style="background-color: white; margin-top: 6rem;">
      <input id="groupedlogs-value" type="hidden" value="<%= groupedLogs %>">
      <input id="login-labels" type="hidden" value="<%= loginLabels %>">
      <input id="login-data" type="hidden" value="<%= loginData %>">
      <div class="container w-100">
        <div class="card mb-4">
          <div class="card-header"><a href="/admin/users/<%= user._id %>" class="text-primary"><%= user.name %></a>'s generation count</div>
          <div class="card-body">
            <canvas id="myChart" height="120"></canvas>
          </div>
          <div class="d-flex justify-content-center align-items-center p-4">
            <a class="custom-link" id="yearly-statistics">View yearly statistics</a>
            <a class="custom-link d-none" id="monthly-statistics">View monthly statistics</a>
          </div>
        </div>
      </div>
      <div class="container w-100">
        <div class="card mb-4">
          <div class="card-header"><a href="/admin/users/<%= user._id %>" class="text-primary"><%= user.name %></a>'s logins (last 7 days)</div>
          <div class="card-body">
            <canvas id="myLoginChart" height="120"></canvas>
          </div>
        </div>
      </div>
    </section>
    
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="/scripts/admin/user-statistics.js"></script>
  </body>
</html>